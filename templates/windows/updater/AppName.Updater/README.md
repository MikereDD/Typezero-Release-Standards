# AppName.Updater

Implement the separate Windows updater here. It should receive an update transaction containing the verified staging directory, installation path, previous-version backup path, application process identity, relaunch command, and health-check token.
